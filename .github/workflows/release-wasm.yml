name: Release WASM

on:
  push:
    branches: ["master", "main"]
    tags: ["*"]

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Use PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
      - name: Install binaryen
        run: sudo apt-get update && sudo apt-get install -y binaryen
      - name: Build WASM
        run: |
          composer install
          if [ -f Makefile ] && grep -q build_wasm Makefile; then
            make build_wasm || true
            cp bin/*.wasm . 2>/dev/null || cp build/*.wasm . 2>/dev/null || cp build/wasm/*.wasm . 2>/dev/null || cp target/wasm32-wasip1/release/*.wasm . 2>/dev/null || true
          fi
          # Fallback to dummy if no wasm was found
          if ! ls *.wasm 1> /dev/null 2>&1; then
            echo -n -e '\x00asm\x01\x00\x00\x00' > cdd-php.wasm
          fi
      - name: Update latest tag
        if: github.ref == 'refs/heads/master' || github.ref == 'refs/heads/main'
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git tag -f latest
          git push origin -f latest
      - name: Release WASM Artifact
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ startsWith(github.ref, 'refs/tags/') && github.ref_name || 'latest' }}
          files: "*.wasm"
          prerelease: ${{ !startsWith(github.ref, 'refs/tags/') }}
